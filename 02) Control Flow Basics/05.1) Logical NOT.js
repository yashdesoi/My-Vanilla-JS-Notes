// Logical NOT in python "not", e.g. if not True:
// Logical NOT in javascript "!", e.g. if (!true) {}

let loggedIn = false;

if (!loggedIn) {
    console.log('Show sign-in page');
} else {
    console.log("Show user's profile");
}
